#include "controlData.hpp"
#include <filesystem>

controlData::controlData(const std::string workingDir) : workingDir_(workingDir)
{
    // std::ifstream json_stream("params.json");
    // nlohmann::json myParams;
    // json_stream >> myParams;
}
